[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "hanziocr"
version = "1.0.0"
dependencies = []

[project.scripts]
hanziocr = "app:main"

[tool.setuptools]
py-modules = ["app", "cv", "ocr", "pinyin", "screenshot", "translate"]